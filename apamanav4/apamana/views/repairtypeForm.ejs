<% include _header %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-9">
      <div class="card">
        <div class="card-body">
          <% if(data) { %>
            <form action="/repairtype/update/<%=data[0].id%>" method="post">
          <% } else { %>
            <form action="/repairtype/add" method="post">
          <% } %>
          <div class="form-group">
          <label>ชื่อประเภทอุปกรณ์ </label>
          <% if(data){ %>
          <input type="text" name="name" value="<%= data[0].name %>"  class="form-control">
        <% } else { %>
          <input type="text" name="name" value="" placeholder="" class="form-control">
        <% } %>
      </div>
      <div class="form-group" hidden>
        <label>apartment</label>
        <% if(data1){ %>
          <input type="text" name="apartment_id" value="<%= data1[0].id %>" class="form-control">
        <% } else { %>
          <input type="text" name="apartment_id" value="<%= data1[0].id %>" placeholder="apartment" class="form-control">
        <% } %>
      </div>
      <div style ="text-align:center">
      <button type="submit" class="btn btn-success">[บันทึก]</button>
      <a href="/repairtype/new" class="btn btn-warning">[แก้ไข]</a>
      </div>
      </div>
    </div>
  </div>
</div>


<% include _footer %>
