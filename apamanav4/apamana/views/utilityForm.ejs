<% include _header %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-7">

      <% if(data2) { %>
        <form action="/utility/update/<%= data2[0].id %>" method="post">
      <%  }else { %>
        <form action="/utility/add" method="post">
    <%  } %>

    <div class="form-group">
      <% if(data2){ %>
        <input type="text" name="name" value="<%= data2[0].name %>" class="form-control">
      <% }else { %>
      <input type="text" name="name" placeholder="Name" class="form-control">
    <% } %>
    </div>


    <div class="form-group">
          <label> package </label>
          <select class="form-control" name="package">
            <option> กรุณาเลือกประเภท </option>
            <option value="0"  > ราคาต่อหน่วย </option>
            <option value="1"  > เหมาจ่าย </option>

          </select>
        </div>

    <div class="form-group">
      <% if(data2){ %>
        <input type="text" name="price" value="<%= data2[0].price %>" class="form-control">
      <% }else { %>
      <input type="text" name="price" placeholder="Price" class="form-control">
    <% } %>
    </div>

    <div class="form-group" hidden>
      <label>apartment</label>
      <% if(data1){ %>
        <input type="text" name="apartment_id" value="<%= data1[0].id %>" class="form-control">
      <% } else { %>
        <input type="text" name="apartment_id" value="<%= data1[0].id %>" placeholder="apartment" class="form-control">
      <% } %>
    </div>

      <button type="submit" class="btn btn-primary">Add Utility</button>
      </form>

      </div>
      </div>
      </div>

<% include _footer %>
